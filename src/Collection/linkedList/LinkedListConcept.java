package Collection.linkedList;

public class LinkedListConcept {
    /*
    1) LinkedList accept duplicate value
    2) LinkedList maintain insertion order
    3) LinkedList can be used as a list, stack or queue.
    4) LinkedList is non-synchronized
    5) LinkedList manipulation is very fast because no shifting needs to occur.
    */
}
